The analytics pipeline code was used for a project for my masters. Both files associated with it (Business mock dataset; Predicted data) are mock datasets. 


Similary, the mock relational database was used for a project and is entirely mock data. 
